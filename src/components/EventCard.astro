---
interface Props {
  title: string;
  theme: string;
  slug: string;
}

const { title, theme, slug } = Astro.props;
---

<a href={slug}>
  <div class="p-5 border max-w-[500px]">
    <h1>{title}</h1>
    <p>{theme}</p>
  </div>
</a>
